def plusOne(self, digits):
        """
        :type digits: List[int]
        :rtype: List[int]
        """
        
        
        
        return[ int(i) for i in str(int(''.join(map(str,digits)))+1 )]  
        
        """
        
        number = int(''.join(str(i) for i in digits))
        number+= 1
        r_list = []
    
        while(number >0):
            r_list.append(number%10)
            number = number//10
        
       
        return r_list[::-1]
        """
